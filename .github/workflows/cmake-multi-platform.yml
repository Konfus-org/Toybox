# This workflow is for a CMake project running on multiple platforms.
name: CMake on multiple platforms

on:
  push:
    branches: [ "main" ]
    paths-ignore:
      - '.github/labeler.yml'
      - '**/*.md'
  pull_request:
    branches: [ "main" ]
    paths-ignore:
      - '.github/labeler.yml'
      - '**/*.md'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: ./.github/workflows/cmake-platform-build.yml
    with:
      matrix-config: >-
        {"include":[
          {"os":"windows-latest","configure_preset":"msvc-tests","build_preset":"msvc-release-tests","test_preset":"test-msvc-release"},
          {"os":"ubuntu-latest","configure_preset":"clang-tests","build_preset":"clang-release-tests","test_preset":"test-clang-release"}
        ]}
