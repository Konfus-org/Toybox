#pragma once
// Jolt requires <Jolt/Jolt.h> before any other JPH header. This shim's quoted include sorts
// ahead of every other include line, so alphabetical include sorting can never break that.
#include <Jolt/Jolt.h>
